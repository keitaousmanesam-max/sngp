{
  "build": {
    "command": "composer install --optimize-autoloader --no-scripts --no-interaction --ignore-platform-req=ext-gd"
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port="
  }
}
