{
  "php": {
    "extensions": ["gd", "mbstring", "xml", "zip", "bcmath", "mysql", "pdo_mysql"]
  },
  "nodejs": {
    "install": false
  },
  "build": {
    "command": "composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port="
  }
}
