{
  "phases": {
    "build": {
      "dependsOn": ["install:composer"],
      "commands": [
        "install-php-extensions gd"
      ]
    }
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port="
  }
}
